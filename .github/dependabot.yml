version: 2
updates:
  # Dependabot uses "pip" for Poetry projects (pyproject.toml + poetry.lock).
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    allow:
      - dependency-type: "direct"
      - dependency-type: "development"
